<%= simple_form_for [@cocktail, @dose], html: { class: 'form-inline' } do |f| %>
  <%= f.submit "Add Ingredient", class: 'submit btn' %>
  <%= f.association :ingredient, label: false %>
  <%= f.input :description, label: false, placeholder: "unit type" %>
<% end %>
